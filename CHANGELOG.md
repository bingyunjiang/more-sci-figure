# 变更记录

本文件记录 `more-sci-figure` 的重要变更。

## 0.3.1 - 2026-07-26

### 新增

- 增加 `review-assess`：融合来源完整性、候选哈希、坐标标定、质量门、系列覆盖率/缺口、引导残差、像素支持和模型辅助比例，输出 0–100 综合评分、风险等级、系列摘要与异常组。
- 增加 `review-confirm`：仅当异常清单为空时，用户在对话中回复“下一步/继续”即可生成覆盖全部普通候选的哈希绑定批量确认。
- 增加 `review-assessment.json` 和 `review-anomalies.csv`；综合评估不推进正式复核状态，高危问题禁止一键确认。
- 异常候选从普通批量数据中彻底分离：每项显示局部原图证据、坐标、异常原因和独立决定；普通候选默认批量接受，但任何异常未决都会阻止生成复核文件，`review-confirm` 也拒绝越过非空异常清单。
- 默认复核流程改为异常优先；`review.html` 的逐点表成为默认折叠的高级深挖工具，不再要求用户逐点点击大量候选。
- 增加 `anomaly_first_split_review` 审计类型，复核记录明确保存异常候选编号并声明其与普通批次分离。

### 修正

- 修正综合评估只显示异常数量、无法查看或决定异常点的问题。
- 修正异常候选可能与普通候选一起被批量确认的问题。
- 当前正式项目以只读方式显示历史决定，避免页面覆盖已应用的证据链。

### v0.3.1 持续增强范围

- 建立带来源、授权和标注协议的真实图表基准集。
- 增加可审计 OCR 候选、半自动图例定位和逐系列误检/漏检指标。
- 在真实基准证据充分后扩展更多图表类型；以上内容继续记入 v0.3.1，不另设新版本号。

## 0.3.0 - 2026-07-26

### 新增

- 增加 `guided_path` 同色曲线提取：稀疏引导点只限定搜索走廊，候选仍要求真实像素支持。
- 增加 `guided_group_path`：同色系列联合优化且排他分配像素，支持形状保持引导、连续性代价及 `solid`/`dashed` 语义。
- 增加按实线/虚线分别声明覆盖率和最大缺口的质量门。
- 增加逐系列 `exclude_boxes_px`，避免整块图例排除误删数据轨迹。
- 增加 `ambiguous_shared_colour`，显式标识同色引导走廊重叠证据。
- 增加带水印 `preview` 命令；候选预览不会生成 `data.csv` 或推进清单状态。
- 复核页把复核人、批量按钮和实时统计集中到逐项决策面板；候选默认待决策，接受或拒绝后提供行色、状态标签和顶部提示。
- 复核页明确区分“页面决策、JSON 生成、本地保存、正式应用”四步，并提供可复制的 `review-apply` 或完整管线命令。
- 移除易导致应用内浏览器崩溃的 `Blob` 自动下载和紧接的模态弹窗；增加只监听本机且带随机令牌的 `review-serve`，固定保存到当前项目目录的 `review-decisions.json`，不再让用户选择保存路径，只有写入完成才报告实际路径、哈希和保存成功。
- 移除复核页全部原生 `alert`/`confirm`；复核人改为强制必填，并以禁用按钮和内联状态强制待决策、模型辅助项确认及生成范围确认。
- 大批量复核 JSON 改为分批生成并显示进度，避免一次性扫描大量候选导致页面长时间无响应。
- 保存后的继续路线改为 Codex、Claude Code、Hermes 等 Agent 可直接执行的中文任务语句，提供完整管线与仅应用复核两种范围，不再展示 Python 命令。
- Agent 交接提示词移除生成机器的绝对路径，改用复核 JSON 的 `candidate_sha256` 唯一匹配候选和项目，并在无匹配或多匹配时强制停止询问。
- 复核 JSON 增加可选 `project_id` 和 `source_sha256` 辅助线索，但候选哈希仍是正式匹配依据。
- 复核页增加一键全部接受、全部拒绝、实时决策统计和歧义候选确认。
- 复核决定增加坐标校正和系列重新归属；正式数据保留原坐标、原系列、理由及动作类型。
- 增加固定像素画布、轴框、逐系列样式和形状保持展示几何。
- 增加独立 `display-geometry.csv`，把派生展示点与人工接受观测值分离。
- 增加 `guide_constrained` 展示几何：以声明引导路径为连续骨架，只用可见像素残差校正，并逐系列记录几何来源。

### 修正

- 修复 `extract` 把项目规格复制到证据目录后未重定位相对来源路径，导致同一规格换目录后失效的问题。
- 修复同色实线/虚线被逐列颜色中位数合并的问题。
- 修复图内图例大范围排除造成真实曲线整段丢失的问题。
- 修复展示层把短 JPEG/虚线缺口直接呈现为断裂曲线的问题，同时继续保留观测层缺口。
- 增加同色曲线、图内图例、受限补线、候选预览和固定画布回归测试。

## 0.2.0 - 2026-07-25

### 新增

- 增加 `candidates.csv → review-decisions.json → data.csv` 人工复核闭环。
- 增加本地中文 `review.html`，支持逐候选接受、拒绝和理由记录。
- 增加 `review` 与 `review-apply` 命令。
- 增加 `review_status`，与提取、重绘和交付状态分离。
- 增加项目级质量门：标定残差、曲线覆盖率、最大缺口、组件数量和拒绝比例。
- 增加候选编号、像素证据、不确定度、散点填充率/长宽比、柱图矩形度/基线距离。
- 增加可选 Lab 颜色距离。
- 增加绘图区 MAE 和残差热图。
- 增加中文 README、协议、CLI 帮助、正式 JSON Schema 和真实基准框架。

### 修正

- 曲线重绘按 `segment_break` 分段，不再跨缺失区域连线。
- 重绘应用项目声明的线性或对数坐标尺度。
- 纯数据重绘使用 `not_applicable` 提取/复核状态，可以正常通过交付验证。
- 不再声称支持实际无法读取的旧版 `.xls`。
- 拒绝锚点数值相同、NaN、无穷值和退化斜率。
- 对数纵轴柱图缺少显式基线时给出明确诊断。
- 中文标签增加常见中文字体回退。

### 行为变化

- `extract` 不再直接生成正式 `data.csv`。
- `pipeline` 缺少复核文件时会停在 `awaiting_review`。
- 图像提取项目未经人工复核不能获得 `delivery_status=pass`。

## 0.1.0 - 2026-07-24

- 首个本地版本。
- 支持来源检查、线图/散点图/柱图候选提取、PNG/SVG/PDF 重绘和基础交付验证。
